import Logo from "./Components/Logo/Logo"
import { SignIn } from "./Pages/Sign-in"

export const Pablic = ()=> {
return (
    <div className="container" >
        <Logo />
        <SignIn/>
    </div>
)
}